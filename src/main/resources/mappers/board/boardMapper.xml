<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.boarddemo.board.mapper.BoardMapper">
    <select id="getList" resultType="com.example.boarddemo.board.dto.BoardDto">
        select id
             , title
             , content
          from BOARD
    </select>
</mapper>